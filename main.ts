export {} // make this a module

let str = 'asdf';
console.log(str);

let b :boolean = true;
let n :number = 0;
let name :string= ''

function add(n1: number, n2: number): number {
    return n1 + n2;
}

class C{
    n: number;
}

function f(c: C){
    return
}